# Go Secure Coding Practice

보안 코딩 연습을 위한 시작 프로젝트입니다.

처음부터 구조를 예쁘게 나누기보다, `cmd/server/main.go` 하나에 코드를 모아 둔 상태에서
먼저 흐름을 이해하고 직접 분리 기준을 고민할 수 있게 만드는 것이 목적입니다.

지난 과제와 수업에서 설명했던 내용, 그리고 전달된 가이드 문서를 떠올리면서
어떤 기능부터 구현하고 어떤 기준으로 나눌지 스스로 판단해 보세요.

## 프로젝트 목적

- 로그인 흐름을 먼저 이해하기
- 더미 API를 실제 동작 코드로 바꿔 보기
- 게시판과 금융 기능을 단계적으로 채워 보기
- 입력 검증, 인증 확인, 권한 검사, 응답 설계를 직접 고민해 보기
- 구현 후 어떤 코드들을 묶어 리팩터링할지 판단해 보기

## 현재 상태

- 서버 코드는 현재 `cmd/server/main.go` 하나에 들어 있습니다.
- 로그인만 SQLite 조회를 사용합니다.
- 로그인 성공 시 `authorization` 쿠키와 `Authorization` 헤더용 토큰을 함께 사용할 수 있습니다.
- 세션은 DB가 아니라 메모리 맵으로 관리합니다.
- 게시판 API는 고정 더미 응답을 반환합니다.
- 금융 API는 더미 응답을 반환합니다.
- 정적 화면은 SPA 형태로 준비되어 있습니다.

## 기본 계정

- `alice / alice1234`
- `bob / bob1234`
- `charlie / charlie1234`

## 주요 API

인증
- `POST /api/auth/register`
- `POST /api/auth/login`
- `POST /api/auth/logout`
- `POST /api/auth/withdraw`

사용자
- `GET /api/me`

게시판
- `GET /api/posts`
- `GET /api/posts/:id`
- `POST /api/posts`
- `PUT /api/posts/:id`
- `DELETE /api/posts/:id`

금융
- `POST /api/banking/deposit`
- `POST /api/banking/withdraw`
- `POST /api/banking/transfer`

## 참고 파일

- `schema.sql`
- `seed.sql`
- `query_examples.sql`

## 먼저 해볼 작업

1. 회원가입 더미 핸들러를 실제 `INSERT`로 바꿔 보기
- binding으로 검사, 맞지 않을 시 에러출력
2. 게시글 작성, 조회, 수정, 삭제를 실제 SQL 또는 원하는 저장 방식으로 바꿔 보기
3. 입금, 출금, 송금 더미 핸들러를 실제 로직으로 바꿔 보기
4. 실패 조건과 입력 검증 규칙을 정리해 보기
5. 반복되는 인증 확인 코드를 어떻게 줄일지 생각해 보기

## 작업하면서 점검할 질문

- 이 코드는 요청 처리인지, 비즈니스 규칙인지, DB 접근인지?
- 같은 인증 확인 코드가 반복되고 있지 않은가?
- 게시글 수정/삭제 권한 검사는 어디에 두는 것이 자연스러운가?
- 출금과 송금에서 어떤 실패 조건을 먼저 막아야 하는가?
- 어떤 응답은 그대로 내려도 되고, 어떤 응답은 가공이 필요한가?
- 언제부터 `handler`, `service`, `store` 같은 구조로 나누는 것이 좋은가?

## 실행 방법

프로젝트 루트에서 실행합니다.

```powershell
go run ./cmd/server
```
처음 상태로 다시 시작하고 싶으면 `app.db`를 지운 뒤 다시 실행하면 됩니다.

### 작업한 내용의 특징에 대해서 작성해주세요
> main.go:141
> 동시에 같은 아이디로 가입 요청이 들어와 데이터가 꼬이는 걸 막기 위해, 서버에서 중복 검사를 하는 대신 DB의 UNIQUE 제약 조건을 활용해 에러를 튕겨내도록 처리했다.
>
> main.go:181
> 자바스크립트로 로그인 토큰을 탈취하는 공격을 막으려고, 쿠키에 HttpOnly와 SameSiteLaxMode 옵션을 걸어 안전하게 보관했다.
>
> main.go:246
> DB에서 유저 정보를 지운 즉시 서버 메모리의 세션을 삭제하고, 브라우저의 쿠키까지 만료시켰다.
>
> main.go:281
> 마이너스 금액을 입금해서 잔고를 깎아내리거나 0원을 입금해 시스템 리소스를 낭비하는 걸 막으려고, Amount <= 0일 때 에러를 뱉게 해서 비정상적인 요청을 막았다.
>
> main.go:312
> DB만 바꾸면 사용자가 새로고침하기 전까지 옛날 잔액이 보이는데, 서버 메모리에 있는 세션 정보까지 바로 업데이트해서 화면에 즉시 바뀐 잔액이 나오도록 구성했다.
>
> main.go:399
> 0원 이하나 음수 금액을 요청해서 시스템을 교란하려는 시도를 차단했다.
>
> main.go:571
> 게시글 데이터와 유저 데이터를 실시간으로 합쳐서 보여주기 때문에, 작성자가 닉네임을 바꿔도 상세 페이지에서 항상 최신 이름이 나오도록 했다.
>
> main.go:635
> 만약 수정한 행의 개수가 0이라면, 글이 없거나 본인 글이 아니라는 뜻이다. 이럴 때 그냥 넘어가지 않고 에러를 줘서 수정 권한이 없다는 사실을 사용자에게 정확히 알려주게 구성했다.


### 느낀 점
> 단 하루라는 짧은 시간 동안 뱅킹 시스템부터 게시판 보안까지 그 방대한 로직을 몰아치듯 구현해냈다는 사실이 스스로도 놀라울 만큼 몰입감이 엄청난 하루였다. 처음에는 단순히 입출금과 게시글 작성이 돌아가게 만드는 데 급급했을 수도 있었겠지만, 코드를 한 줄씩 써 내려가며 생각보다 내가 작성한 코드에 허점이 많다는 걸 알고 이를 막으려 계속 고민한 결과가 트랜잭션 처리나 잔액 검증 같은 보안으로 이어지는 과정이 정말 신기했다. 특히 송금 기능에서 발생할 수 있는 데이터 꼬임 문제를 막기 위해 DB 레벨에서 원자적 업데이트를 고민하고, 게시판 수정 권한을 체크하며 주소창 조작 공격을 차단하는 로직을 입힐 때는 개발자로서의 책임감이 단순히 기능 구현 그 이상에 있다는 것을 뼈저리게 실감했다. 어느덧 1,000줄을 향해가는 main.go의 비대해진 몸집을 보면서 이제는 직관적인 유지보수를 위해 구조적인 분리가 선택이 아닌 필수라는 점을 깨달았으며, 오늘 하루 쏟아부은 에너지가 단순히 돌아가는 프로그램을 만든 것을 넘는 경험이라 정말 좋았다.
